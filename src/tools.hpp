#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <filesystem>
#include <format>

std::vector<std::string> SplitString(const std::string& str, char delimiter, bool tokenize = false);